#!/bin/sh /etc/rc.common

# Needs to be started after network!
START=99
STOP=99

boot() {
  /usr/bin/sync-settings | logger -t "static-routes"
  /etc/init.d/network reload | logger -t "static-routes"
  /usr/bin/notify-mtu-sysdb | logger -t "static-routes"
}
