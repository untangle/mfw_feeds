version: '2'
services:
  build:
    image: untangleinc/mfw:build
    environment:
      BUILD_URL:
    volumes:
      - ../:/home/mfw/openwrt
  build-local-container:
    build:
      context: .
      dockerfile: Dockerfile.build
    volumes:
      - ../:/home/mfw/openwrt

